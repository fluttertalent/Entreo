namespace WebApp.Entreo.Models.Enums
{
    public enum ObstacleImpact
    {
        Negligible = 0,    // Minimal impact on habit formation
        Minor = 1,         // Slightly affects habit performance
        Moderate = 2,      // Noticeably impacts habit consistency
        Major = 3,         // Significantly disrupts habit formation
        Severe = 4,        // Prevents habit formation entirely
        Variable = 5,      // Impact varies depending on circumstances
        Cascading = 6      // Affects multiple related habits
    }
}